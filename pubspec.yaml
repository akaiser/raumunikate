name: raumunikate
description: Raumunikate
version: 0.0.1

environment:
  sdk: ">=2.19.2 <3.0.0"
  flutter: ">=3.7.5"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.2
  go_router: ^6.2.0
  provider: ^6.0.5

dev_dependencies:
  dart_code_metrics: ^5.6.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^4.0.0+1

flutter:
  assets:
    - assets/
  uses-material-design: true
